import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('Uygulama başlıyor', (WidgetTester tester) async {
    // Firebase gerektirdiğinden smoke test placeholder olarak bırakıldı.
    expect(true, isTrue);
  });
}
